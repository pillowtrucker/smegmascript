# AT Protocol (Bluesky) Credentials
BSKY_IDENTIFIER=your-bot-handle.bsky.social
BSKY_PASSWORD=your-app-password
BSKY_SERVICE=https://bsky.social

# Queue Mode (true for production with Redis, false for simple direct mode)
USE_QUEUE=true

# Admin DIDs (comma-separated list of DIDs that can use admin commands)
ADMIN_DIDS=did:plc:example123456789,did:plc:another123456

# Redis Configuration (only used if USE_QUEUE=true)
REDIS_HOST=redis
REDIS_PORT=6379
